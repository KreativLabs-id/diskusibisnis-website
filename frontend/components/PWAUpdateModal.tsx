'use client';

// PWA Update Modal - Disabled
// Modal ini dinonaktifkan untuk menghilangkan popup "Update Tersedia"
export function PWAUpdateModal() {
  // Langsung return null untuk menonaktifkan modal
  return null;

  /*
  // Kode modal yang dinonaktifkan - simpan untuk referensi jika ingin diaktifkan kembali
  return (
    <div className="fixed inset-0 z-[100] flex items-center justify-center p-4 bg-black/50 backdrop-blur-sm animate-fade-in">
      ...
    </div>
  );
  */
}
